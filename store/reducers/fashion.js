import PRODUCTS from '../../data/dummy-data';

const initialState = {
  fashion: PRODUCTS,
};

export default (state = initialState, action) => {
  // switch (type) {
  //   case typeName:
  //     return { ...state, ...payload };

  //   default:
  return state;
  // }
};
